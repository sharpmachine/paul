var tinyMCEImageList = new Array(
        <% @pictures.each do |picture| %>
			["<%= picture.title %> (Large - 640 x 480px)", "<%= picture.image.url(:large) %>"],
			["<%= picture.title %> (Square - 360 x 360px)", "<%= picture.image.url(:square) %>"],						
			["<%= picture.title %> (Medium - 480 x 360px)", "<%= picture.image.url(:medium) %>"],			
			["<%= picture.title %> (Small - 320 x 240px)", "<%= picture.image.url(:small) %>"],						
			["<%= picture.title %> (Thumb - 100 x 75px)", "<%= picture.image.url(:thumb) %>"]<% unless picture == @pictures.last %>,<% end %>									
        <% end %>
);