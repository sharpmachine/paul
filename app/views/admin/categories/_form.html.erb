<%= simple_form_for [:admin, @category], :html => { :class => 'form-horizontal' } do |f| %>
  
<fieldset>
<legend><%= controller.action_name.capitalize %> Category</legend>

		<%= f.input :name %>
	
</fieldset>

<div class="form-actions">

    	<%= link_to "Cancel", admin_categories_path, :class => "btn small grey" %> <%= f.button :submit, :class => 'btn btn-primary' %>
</div>
<% end %>
