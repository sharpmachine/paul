<%= form_for([:admin, @tag], remote: true, id: "new_tag", :html => { :class => 'form-inline' }) do |f| %>

    <%= f.text_field :tagstring, :class => 'input-medium' %>

    <%= f.submit "Add", :class => "btn" %>

<% end %>
